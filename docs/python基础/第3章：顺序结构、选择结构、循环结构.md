### Python 选择结构


!!! quote "参考资料"
    hhu史秋实老师的ppt
    [点击这里查看或下载 PDF 文档](../assets/py3.pdf)




选择结构用于根据条件判断执行不同代码块，核心是**布尔表达式**（条件）。



- **`if` 单分支**：仅当条件为 `True` 时执行代码块。
  ```python
  if score >= 60:
      print("及格")
  ```

- **`if-else` 双分支**：条件为 `True` 执行 `if` 块，否则执行 `else` 块。
  ```python
  if score >= 60:
      print("及格")
  else:
      print("不及格")
  ```

- **`if-elif-else` 多分支**：依次判断条件，执行第一个为 `True` 的块，最后 `else` 可选。
  ```python
  if score >= 90:
      print("优秀")
  elif score >= 80:
      print("良好")
  elif score >= 60:
      print("及格")
  else:
      print("不及格")
  ```

- **三元表达式**：简洁版 `if-else`，用于简单赋值。
  ```python
  result = "及格" if score >= 60 else "不及格"
  ```


### Python 循环结构
循环结构用于重复执行代码块，核心是**循环条件**或**可迭代对象**。

- **`for` 循环**：遍历可迭代对象（如列表、字符串、`range()`），依次取元素执行。
  ```python
  # 遍历列表
  for name in ["Alice", "Bob"]:
      print(name)
  
  # 配合 range() 执行固定次数
  for i in range(5):  # 0-4
      print(i)
  ```

- **`while` 循环**：只要条件为 `True` 就重复执行，需注意避免死循环。
  ```python
  count = 0
  while count < 5:
      print(count)
      count += 1  # 必须更新条件，否则死循环
  ```

- **循环控制关键字**：
  - `break`：**立即跳出整个循环**，不再执行剩余迭代。
  - `continue`：**跳过当前循环的剩余代码**，直接进入下一次迭代。

- **循环的 `else` 子句**：当循环**正常结束**（未被 `break` 中断）时执行。
  ```python
  for i in range(3):
      if i == 5:
          break  # 不会触发，因为 i 最大为 2
  else:
      print("循环正常结束")
  ```
!!! tip 
    [python中的迭代器](https://blog.csdn.net/m0_74091159/article/details/144321721?ops_request_misc=elastic_search_misc&request_id=12c19a71b90a29b676194829c565e193&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-144321721-null-null.142^v102^pc_search_result_base2&utm_term=python%E4%B8%AD%E7%9A%84%E8%BF%AD%E4%BB%A3%E5%99%A8&spm=1018.2226.3001.4187)