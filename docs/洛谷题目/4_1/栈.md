# P1044 [NOIP 2003 普及组] 栈

## 题目背景

栈是计算机中经典的数据结构，简单的说，栈就是限制在一端进行插入删除操作的线性表。

栈有两种最重要的操作，即 pop（从栈顶弹出一个元素）和 push（将一个元素进栈）。

栈的重要性不言自明，任何一门数据结构的课程都会介绍栈。宁宁同学在复习栈的基本概念时，想到了一个书上没有讲过的问题，而他自己无法给出答案，所以需要你的帮忙。

## 题目描述

![](https://cdn.luogu.com.cn/upload/image_hosting/5qxy9fz2.png)

宁宁考虑的是这样一个问题：一个操作数序列，$1,2,\ldots ,n$（图示为 1 到 3 的情况），栈 A 的深度大于 $n$。

现在可以进行两种操作，

1. 将一个数，从操作数序列的头端移到栈的头端（对应数据结构栈的 push 操作）
2. 将一个数，从栈的头端移到输出序列的尾端（对应数据结构栈的 pop 操作）

使用这两种操作，由一个操作数序列就可以得到一系列的输出序列，下图所示为由 `1 2 3` 生成序列 `2 3 1` 的过程。

![](https://cdn.luogu.com.cn/upload/image_hosting/8uwv2pa2.png)

（原始状态如上图所示）

你的程序将对给定的 $n$，计算并输出由操作数序列 $1,2,\ldots,n$ 经过操作可能得到的输出序列的总数。

## 输入格式

输入文件只含一个整数 $n$（$1 \leq n \leq 18$）。

## 输出格式

输出文件只有一行，即可能输出序列的总数目。

## 输入输出样例 #1

### 输入 #1

```
3

```

### 输出 #1

```
5

```

## 说明/提示

**【题目来源】**

NOIP 2003 普及组第三题


!!! tip
    [B站讲解](https://www.bilibili.com/video/BV1o94y1y7yB/)  
    这题背后设计到卡特兰数,有兴趣的可以看下面的文章    
    [b站](https://www.bilibili.com/video/BV1wr4y1q7qw/)  
    [CSDN](https://blog.csdn.net/sherry_yue/article/details/88364746)  



## 📌 题目分析

给定序列：

```text
1,2,3,...,n
```

通过栈的 `push / pop` 操作，问一共能产生多少种不同输出序列。

👉 本质：

```text
合法出栈序列数量
```

👉 类型：

```text
经典 Catalan 数（卡特兰数） / DP
```



## 🧠 解题思路

## 1️⃣ 为什么是卡特兰数

每个数字都要经历：

```text
入栈一次
出栈一次
```

共 `2n` 次操作。

并且任意时刻必须满足：

```text
已出栈数 ≤ 已入栈数
```

也就是：

```text
栈不能为空才能 pop
```

这类“合法括号序列 / 合法进出栈方案数”就是经典 **Catalan 数**。



## 2️⃣ 卡特兰递推公式

设：

```text
dp[n] = n 个数的合法出栈序列数量
```

递推：

```text
dp[0] = 1
dp[n] = dp[0]*dp[n-1] + dp[1]*dp[n-2] + ... + dp[n-1]*dp[0]
```

即：

```text
dp[n] = Σ dp[i] * dp[n-1-i]
```



## 3️⃣ 为什么 n ≤ 18 可以直接 DP

数据范围很小：

```text
n ≤ 18
```

Catalan(18)：

```text
477638700
```

`long long` 足够。



### 4️⃣ 时间复杂度

两层循环：

```text
O(n²)
```

最大仅 18，极快。

---

## 💻 C++代码

```cpp
#include <bits/stdc++.h>
using namespace std;

long long dp[25];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    dp[0] = 1;

    for (int i = 1; i <= n; i++) {
        for (int j = 0; j < i; j++) {
            dp[i] += dp[j] * dp[i - 1 - j];
        }
    }

    cout << dp[n];

    return 0;
}
```

---

## ⚠️ 易错点

### ❌ 1. dp[0] 忘记初始化

必须：

```cpp
dp[0] = 1;
```

它表示空序列也算一种方案。


### ❌ 2. 用 int

虽然这题勉强够，但建议统一：

```cpp
long long
```



### ❌ 3. 误以为要模拟栈

这题如果暴力枚举所有出栈序列会非常复杂。

实际上考的是：

```text
数学规律 + DP
```



## 🚀 一句话总结

```text
合法出栈序列数量 = 第 n 个卡特兰数
```

---

## 🔥 补充

卡特兰数出现的场景：

* 合法括号序列数量
* 栈的出栈方案数
* 二叉树形态数
* 凸多边形划分数

---



