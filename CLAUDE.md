# CLAUDE.md

本项目是 xurry 的个人知识库笔记，使用 **MkDocs + Material for MkDocs** 构建为静态网站，部署在 `https://www.xurry.xyz`。

## 项目结构

```
/Users/xurry/Documents/mynotes/
├── mkdocs.yml          # MkDocs 配置文件
├── docs/               # 所有 Markdown 源文件
│   ├── 模式识别/
│   ├── c++基础/
│   ├── python基础/
│   └── ...
├── site/               # 本地构建输出（不要手动编辑）
└── .github/workflows/  # GitHub Actions 自动部署
```

## MkDocs 排版规则（关键！）

以下规则适用于 `docs/` 下所有 `.md` 文件，确保网页渲染正确。

### 1. 换行与分段

- **软换行（同段落内换行但不分段）：** 行尾加 **两个空格** `  ` 再回车
- **段落分隔：** 两段之间**空一行**（一个空行）

示例：
```
第一行文字··
第二行文字（同一段落，网页上换行但不留大间距）

新段落文字（与上面有段间距）
```
> `··` 表示两个空格。

### 2. 题目编号（防止 Markdown 自动重新编号）

使用 **`1\.` `2\.`**（反斜杠转义点号）代替 `1.` `2.`，否则 Markdown 会把所有编号都渲染成 `1.`。

示例：
```
1\. 模型出现过拟合时，其训练误差与测试误差的典型表现为（）··
A.训练误差大，测试误差大··
B.训练误差小，测试误差大··
C.训练误差小，测试误差小··
D.训练误差大，测试误差小

**答案：B**
```

注意：每个选项行尾也需要两个空格换行，选项之间不空行。

### 3. 选择题选项排版

- A/B/C/D 每个选项单独一行，行尾两个空格
- 选项之间**不空行**（同段落）
- 选项与题干之间**不空行**
- 答案与选项之间**空一行**

### 4. 二级标题与题序

- 每个 `## 标题` 下的题目独立编号，从 **1** 开始
- 标题与第一道题之间空两行

### 5. 解析格式

```
**答案：X**

**解析：**

内容...
```

### 6. 表格

标准 Markdown 表格，列对齐用 `|------|` 即可。

### 7. Admonition 提示框

```
!!! danger "tip"
    内容第一行（缩进4空格）··
    内容第二行
```

### 8. 图片路径

使用 `docs/` 目录下的相对路径：
```
![](./resources/xxx.png)
```

### 9. 分隔线

题与题之间用 `---` 分隔，上下各空一行。

### 10. 构建与部署

- 本地预览：`mkdocs serve`（在 `/Users/xurry/Documents/mynotes` 目录下执行）
- 部署：push 到 `main` 分支后，GitHub Actions 自动执行 `mkdocs gh-deploy`
- 注意：`mkdocs.yml` 中的 `nav` 配置需要显式添加新文件才能出现在网站导航栏中
